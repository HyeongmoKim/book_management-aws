version: 0.2

phases:
  install:
    # 런타임 버전을 지정하여 Node.js와 npm 버전을 맞춥니다.
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Checking files..."
      - ls -al  # package-lock.json이 실제로 있는지 로그로 강제 확인
      - echo "Installing dependencies..."
      # 버전 문제가 계속될 경우를 대비해 npm install로 변경하거나 설정을 추가합니다.
      - npm install -g npm@latest  # npm을 최신으로 업데이트
      - npm ci || npm install      # ci가 실패하면 install로 대체 실행 (안전장치)

  pre_build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com

  build:
    commands:
      - echo "Build started on `date`"
      - npm run build
      - echo "Building the Docker image..."
      - docker build -t $REPOSITORY_URI:latest .
      - docker tag $REPOSITORY_URI:latest $REPOSITORY_URI:$IMAGE_TAG

  post_build:
    commands:
      - echo "Pushing the Docker image..."
      - docker push $REPOSITORY_URI:latest
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      - echo "Deploying to EKS..."
      - aws eks update-kubeconfig --region $AWS_DEFAULT_REGION --name a084110-EKSCluster
      - kubectl apply -f k8s/deployment.yaml